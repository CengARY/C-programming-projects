#include <stdio.h>
int main(void){
    int dice1;
    int dice2;
    int total;
    scanf("%d\n", &dice1);
    scanf("%d", &dice2);
    total = (dice1 + dice2);
    if(total >= 10){
        printf("Special tax \n36");
    }else {
        printf("Regular tax \n%d",2*total);
    }return 0;
}
