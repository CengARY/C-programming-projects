#include <stdio.h>//first
int main(void){
    int number;
    int i;
    char word1[100];
    char word2[100];
    scanf("%d", &number);
    for(i = 0; i < number; i++){
        scanf("%s %s", word1, word2);
        printf("%s %s\n", word2, word1);
    }
    return 0;
}
