#include <stdio.h>
int main(void){
    int i; 
    int first_number = 0;
    for(i = 0; i < 11; i++){
        printf("%dx8 = %d\n", first_number, first_number*8);
        first_number = first_number + 1;
    }
    return 0;
}
