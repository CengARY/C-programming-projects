#include <stdio.h>
int main(void){
    int money = 0;
    int not;
    int sum = 0;
    not = money != -1;
    while(not){
        scanf("%d", &money);
        not = money != -1;
        sum = sum + money;
    }
    printf("%d", sum + 1);
    return 0;
}
